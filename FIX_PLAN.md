# План исправлений на основе анализа кода

## ✅ Уже исправлено:

1. ✅ **Схема `plannedTransactions.status`** - добавлен `.notNull()`
2. ✅ **Типы в тестах** - исправлены для соответствия реальным типам
3. ✅ **`notification.service.ts`** - упрощена проверка `nextDate`
4. ✅ **`notification.repository.ts`** - исправлена сигнатура `createNotification`
5. ✅ **Дубликат в переводах** - удален
6. ✅ **`planned-item-card.tsx`** - убран fallback `|| 'planned'` (больше не нужен)

---

## 🔧 Что нужно исправить:

### 1. **Схема `plannedIncome.status`** - добавить `.notNull()` 🔴 КРИТИЧНО
**Файл:** `shared/schema.ts:167`

**Текущее состояние:**
```typescript
status: text("status").default("pending"), // ❌ Может быть null в TypeScript
```

**Проблема:**
- В БД всегда есть значение (есть дефолт `"pending"`)
- Но TypeScript видит тип как `string | null`
- Код использует `income.status === "pending"` без проверки на null (6 мест в `planned-income-card.tsx`)

**Решение:**
```typescript
status: text("status").default("pending").notNull(), // ✅ Всегда есть значение
```

**Файлы, которые нужно проверить после исправления:**
- `client/src/components/planned-income/planned-income-card.tsx` (6 использований)
- `client/src/pages/planned-income-page.tsx`
- `server/repositories/planned-income.repository.ts`

**Действия:**
1. Добавить `.notNull()` в схему
2. Запустить `npm run check` - проверить, что типы обновились
3. Убедиться, что все использования работают корректно

---

### 2. **`normalize-goal-markers.ts`** - ОСТАВИТЬ fallback ✅ ПРАВИЛЬНО
**Файл:** `client/src/lib/normalize-goal-markers.ts:37`

**Текущее состояние:**
```typescript
status: goal.status || "planned", // ✅ Fallback правильный
```

**Почему это правильно:**
- `TrendGoal` имеет `status?: string` (опциональное поле)
- Это НЕ `PlannedTransaction`, а отдельный тип для графиков
- Fallback нужен, так как поле опциональное
- Это нормальная практика для опциональных полей

**Вывод:** Ничего менять не нужно ✅

---

### 3. **Типизация `transactionData`** - добавить явный тип 🟡 ВАЖНО
**Файл:** `shared/schema.ts:196`

**Текущее состояние:**
```typescript
transactionData: jsonb("transaction_data"), // ❌ Тип: unknown | null
```

**Проблема:**
- `jsonb` имеет тип `unknown`, что небезопасно
- Нет валидации структуры данных
- Нет автодополнения в IDE
- В тестах приходится использовать `as Record<string, any>`

**Решение:**
Создать Zod схему для валидации и типизации:

```typescript
// В shared/schema.ts после импортов
export const notificationTransactionDataSchema = z.object({
  amount: z.string(),
  currency: z.string().default("USD"),
  description: z.string(),
  category: z.string().optional(),
  categoryId: z.number().optional(),
  type: z.enum(["income", "expense"]),
  date: z.string(), // YYYY-MM-DD format
  recurringId: z.number().optional(),
  frequency: z.string().optional(),
  nextDate: z.string().optional(),
});

export type NotificationTransactionData = z.infer<typeof notificationTransactionDataSchema>;
```

**Где использовать:**
1. В `notification.service.ts` при создании уведомлений - валидировать данные
2. В API роутах при валидации входящих данных
3. В тестах - использовать правильный тип вместо `as Record<string, any>`

**Пример использования:**
```typescript
// В notification.service.ts
const transactionData = notificationTransactionDataSchema.parse({
  amount: planned.amount,
  currency: planned.currency || "USD",
  // ...
});
```

---

### 4. **Миграция БД** - проверить существующие данные 🟡 ВАЖНО
**Файл:** `migrations/` (создать новый файл)

**Проверить:**
Если в БД уже есть записи с `status = null`, нужно создать миграцию:

```sql
-- migrations/XXXX_add_not_null_to_status.sql

-- 1. Проверить наличие NULL значений
-- SELECT COUNT(*) FROM planned_transactions WHERE status IS NULL;
-- SELECT COUNT(*) FROM planned_income WHERE status IS NULL;

-- 2. Обновить существующие NULL значения (если есть)
UPDATE planned_transactions 
SET status = 'planned' 
WHERE status IS NULL;

UPDATE planned_income 
SET status = 'pending' 
WHERE status IS NULL;

-- 3. Добавить NOT NULL constraint
ALTER TABLE planned_transactions 
ALTER COLUMN status SET NOT NULL;

ALTER TABLE planned_income 
ALTER COLUMN status SET NOT NULL;
```

**Когда делать:**
- Перед деплоем на production
- После проверки, что нет NULL значений в staging/production БД

---

### 5. **E2E тесты для уведомлений** 🟢 ОПЦИОНАЛЬНО
**Файл:** `tests/e2e/notifications.spec.ts` (создать новый)

**Что добавить:**
- Тест создания уведомления для planned transaction
- Тест чтения уведомления
- Тест удаления уведомления
- Тест фильтрации уведомлений по типу
- Тест фильтрации уведомлений по дате
- Тест отметки уведомления как прочитанного

**Почему:**
- Unit тесты проверяют только UI логику с моками
- E2E тесты проверят реальную интеграцию с БД и API
- Найдут проблемы, которые не видны в unit тестах

---

## 📋 Приоритеты:

### 🔴 P0 (Критично - исправить СЕЙЧАС):
1. ✅ Добавить `.notNull()` для `plannedIncome.status` - **ИСПРАВЛЕНО**
2. ✅ Проверить, что все использования `plannedIncome.status` работают корректно - **ПРОВЕРЕНО (типы обновились автоматически)**

### 🟡 P1 (Важно - исправить в ближайшее время):
3. ✅ Проверить миграцию БД (если есть старые данные с NULL)
4. ✅ Добавить типизацию для `transactionData` (Zod схема)

### 🟢 P2 (Желательно - можно отложить):
5. ✅ Добавить E2E тесты для уведомлений
6. ✅ Улучшить типизацию других `jsonb` полей в схеме

---

## 🎯 План действий (пошагово):

### Шаг 1: Исправить схему `plannedIncome.status` 🔴
```typescript
// shared/schema.ts:167
status: text("status").default("pending").notNull(),
```

**Проверить после:**
- Запустить `npm run check` - должны обновиться типы
- Проверить все использования `plannedIncome.status` в коде:
  - `client/src/components/planned-income/planned-income-card.tsx` (6 мест)
  - `client/src/pages/planned-income-page.tsx`
  - `server/repositories/planned-income.repository.ts`

---

### Шаг 2: Типизация `transactionData` 🟡
1. Создать Zod схему `notificationTransactionDataSchema`
2. Использовать в `notification.service.ts` при создании уведомлений
3. Обновить тесты для использования правильного типа

---

### Шаг 3: Миграция БД 🟡
1. Проверить наличие NULL значений в production/staging БД
2. Если есть - создать миграцию
3. Применить миграцию перед деплоем

---

### Шаг 4: E2E тесты 🟢
1. Создать `tests/e2e/notifications.spec.ts`
2. Добавить тесты для основных операций
3. Проверить интеграцию с реальной БД

---

## 📝 Вывод:

**Что было исправлено правильно:**
- ✅ Типы в тестах приведены в соответствие с реальной схемой
- ✅ Схема `plannedTransactions.status` исправлена
- ✅ Код упрощен где возможно

**Что нужно исправить:**
- 🔴 `plannedIncome.status` - добавить `.notNull()` (1 строка кода)
- 🟡 Типизация `transactionData` - улучшить безопасность типов
- 🟡 Миграция БД - проверить существующие данные

**Принцип:**
- Тесты должны использовать те же типы, что и реальный код ✅
- Схема БД должна отражать реальность (если есть дефолт → `.notNull()`) ✅
- Код должен быть типобезопасным ✅