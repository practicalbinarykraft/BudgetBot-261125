# 🚀 Руководство по настройке админ-панели

**Дата создания:** 2026-01-07  
**Версия:** 1.0.0

---

## 📋 Обзор

Это руководство поможет вам настроить и запустить админ-панель BudgetBot. Админ-панель предоставляет полный контроль над системой: метрики, управление пользователями, аналитика, рассылки и поддержка.

---

## ✅ Предварительные требования

- Node.js 18+ установлен
- PostgreSQL база данных настроена
- Переменная окружения `DATABASE_URL` установлена
- Все зависимости установлены (`npm install`)

---

## 🔧 Шаг 1: Запуск миграций

Первым делом нужно создать все необходимые таблицы в базе данных.

### Вариант 1: Автоматический скрипт (рекомендуется)

```bash
npm run tsx scripts/run-admin-migrations.ts
```

Скрипт автоматически запустит все миграции:
- `0003_create_admin_users.sql` - таблица админов
- `0004_create_admin_audit_log.sql` - таблица аудита
- `0005_create_broadcasts.sql` - таблицы рассылок
- `0006_create_support_chats.sql` - таблицы поддержки

### Вариант 2: Вручную через psql

```bash
psql $DATABASE_URL -f migrations/0003_create_admin_users.sql
psql $DATABASE_URL -f migrations/0004_create_admin_audit_log.sql
psql $DATABASE_URL -f migrations/0005_create_broadcasts.sql
psql $DATABASE_URL -f migrations/0006_create_support_chats.sql
```

### Проверка миграций

После запуска миграций проверьте, что таблицы созданы:

```sql
-- Проверить таблицы
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('admin_users', 'admin_audit_log', 'broadcasts', 'support_chats');
```

---

## 👤 Шаг 2: Создание первого админа

После создания таблиц нужно создать первого администратора.

### Вариант 1: Через скрипт (рекомендуется)

```bash
npm run tsx scripts/create-admin.ts -- --email admin@example.com --password secret123 --role super_admin
```

Параметры:
- `--email` - Email админа (обязательно)
- `--password` - Пароль (минимум 8 символов, обязательно)
- `--role` - Роль: `super_admin`, `support`, `analyst`, `readonly` (по умолчанию: `super_admin`)

### Вариант 2: Через переменные окружения

```bash
ADMIN_EMAIL=admin@example.com ADMIN_PASSWORD=secret123 npm run tsx scripts/create-admin.ts
```

### Вариант 3: Вручную через SQL

```sql
-- Хешируем пароль (используйте bcrypt, например через Node.js)
-- Пример хеша для пароля "secret123": $2a$10$...
INSERT INTO admin_users (email, password_hash, role, permissions, is_active)
VALUES (
  'admin@example.com',
  '$2a$10$...', -- Замените на реальный хеш
  'super_admin',
  ARRAY['*'],
  true
);
```

**⚠️ Важно:** Пароль должен быть захеширован через bcrypt с 10 раундами.

---

## 🔐 Шаг 3: Вход в админ-панель

1. Запустите сервер:
   ```bash
   npm run dev
   ```

2. Откройте админ-панель:
   ```
   http://localhost:3000/app/admin/auth/login
   ```

3. Введите данные созданного админа:
   - Email: `admin@example.com`
   - Password: `secret123`

4. После успешного входа вы попадете на dashboard админ-панели.

---

## 📊 Доступные функции админ-панели

### 1. Dashboard (Главная)
- **URL:** `/app/admin/dashboard`
- **Описание:** Основные метрики системы
- **Метрики:**
  - Общее количество пользователей
  - Активные пользователи (сегодня/неделя/месяц)
  - Доходы и транзакции
  - Метрики роста (MAU, DAU, retention)

### 2. Пользователи
- **URL:** `/app/admin/users`
- **Функции:**
  - Список всех пользователей с фильтрацией
  - Детальная информация о пользователе
  - Транзакции пользователя
  - Timeline активности
  - Действия: начисление кредитов, блокировка

### 3. Аналитика
- **URL:** `/app/admin/analytics`
- **Функции:**
  - Funnel Analysis (воронка конверсии)
  - Feature Adoption (использование фич)
  - User Segments (сегменты пользователей)

### 4. Система
- **URL:** `/app/admin/system/monitoring`
- **Функции:**
  - API performance metrics
  - Database health
  - External services status (Telegram, OpenAI, Redis)
  - Background jobs status

### 5. Рассылки
- **URL:** `/app/admin/broadcasts`
- **Функции:**
  - Создание рассылок
  - Отправка сообщений пользователям
  - Шаблоны рассылок
  - Статистика отправки

### 6. Поддержка
- **URL:** `/app/admin/support`
- **Функции:**
  - Список чатов с пользователями
  - Отправка сообщений
  - Управление статусом чатов

### 7. Audit Log
- **URL:** `/app/admin/audit-log`
- **Описание:** История всех действий админов

---

## 🔑 Роли админов

### super_admin
- **Описание:** Полный доступ ко всем функциям
- **Права:** Все разрешения автоматически
- **Использование:** Главный администратор системы

### support
- **Описание:** Работа с поддержкой пользователей
- **Права:** Просмотр пользователей, отправка сообщений, управление чатами
- **Использование:** Сотрудники поддержки

### analyst
- **Описание:** Доступ к аналитике и метрикам
- **Права:** Просмотр метрик, аналитики, пользователей (read-only)
- **Использование:** Аналитики и маркетологи

### readonly
- **Описание:** Только просмотр
- **Права:** Просмотр всех данных без возможности изменений
- **Использование:** Аудиторы, наблюдатели

---

## 🛡️ Безопасность

### Рекомендации

1. **Пароли:**
   - Минимум 8 символов
   - Используйте сложные пароли
   - Регулярно меняйте пароли

2. **IP Whitelist:**
   - Настройте IP whitelist для критичных админов
   - Используйте поле `ip_whitelist` в таблице `admin_users`

3. **Audit Log:**
   - Все действия админов логируются
   - Проверяйте audit log регулярно

4. **Сессии:**
   - Сессии админов хранятся в PostgreSQL
   - Автоматически очищаются при истечении срока

---

## 🐛 Устранение неполадок

### Проблема: Не могу войти в админ-панель

**Решение:**
1. Проверьте, что миграции выполнены
2. Проверьте, что админ создан
3. Проверьте правильность email и пароля
4. Проверьте, что `is_active = true` в таблице `admin_users`

### Проблема: Ошибка "Unauthorized"

**Решение:**
1. Проверьте, что вы залогинены
2. Проверьте сессию в браузере
3. Попробуйте выйти и войти снова

### Проблема: Метрики не загружаются

**Решение:**
1. Проверьте подключение к базе данных
2. Проверьте логи сервера
3. Попробуйте очистить кэш: `POST /api/admin/metrics/clear-cache`

### Проблема: Рассылки не отправляются

**Решение:**
1. Проверьте, что Telegram бот настроен
2. Проверьте, что у пользователей есть `telegram_id`
3. Проверьте логи сервера

---

## 📝 API Endpoints

### Авторизация
- `POST /api/admin/auth/login` - Вход
- `POST /api/admin/auth/logout` - Выход
- `GET /api/admin/auth/me` - Текущий админ

### Метрики
- `GET /api/admin/metrics/hero` - Основные метрики
- `GET /api/admin/metrics/growth` - Метрики роста
- `POST /api/admin/metrics/clear-cache` - Очистка кэша

### Пользователи
- `GET /api/admin/users` - Список пользователей
- `GET /api/admin/users/:id` - Детали пользователя
- `GET /api/admin/users/:id/transactions` - Транзакции
- `GET /api/admin/users/:id/timeline` - Timeline
- `PATCH /api/admin/users/:id` - Обновление
- `POST /api/admin/users/:id/grant-credits` - Начисление кредитов

### Аналитика
- `GET /api/admin/analytics/funnel` - Воронка конверсии
- `GET /api/admin/analytics/feature-adoption` - Использование фич
- `GET /api/admin/analytics/user-segments` - Сегменты пользователей

### Система
- `GET /api/admin/system/health` - Здоровье системы

### Рассылки
- `GET /api/admin/broadcasts` - Список рассылок
- `POST /api/admin/broadcasts` - Создание рассылки
- `GET /api/admin/broadcasts/:id` - Детали рассылки
- `POST /api/admin/broadcasts/:id/send` - Отправка
- `GET /api/admin/broadcasts/templates` - Шаблоны

### Поддержка
- `GET /api/admin/support/chats` - Список чатов
- `GET /api/admin/support/chats/:id/messages` - Сообщения
- `POST /api/admin/support/chats/:id/messages` - Отправка сообщения
- `PATCH /api/admin/support/chats/:id` - Обновление статуса

---

## 🎯 Следующие шаги

1. ✅ Запустить миграции
2. ✅ Создать первого админа
3. ✅ Войти в админ-панель
4. 📊 Изучить метрики и аналитику
5. 👥 Настроить управление пользователями
6. 📧 Создать шаблоны рассылок
7. 💬 Настроить систему поддержки

---

## 📚 Дополнительная документация

- **План разработки:** `docs/ADMIN_PANEL_BACKEND_PLAN.md`
- **Спецификация:** `docs/ADMIN_PANEL_SPECIFICATION.md`
- **API документация:** `/api-docs` (Swagger UI)

---

**Готово!** Админ-панель настроена и готова к использованию. 🎉

