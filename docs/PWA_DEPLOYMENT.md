# PWA Deployment Guide - Инструкция по обновлению PWA

> **⚠️ КРИТИЧЕСКИ ВАЖНО:** При каждом изменении кода, который должен попасть к пользователям, ОБЯЗАТЕЛЬНО обновить версию кэша Service Worker!

---

## 🔄 Процесс обновления PWA при изменении кода

### Шаг 1: Обновить версию кэша в Service Worker

**Файл:** `client/public/sw.js`

```javascript
// БЫЛО:
const CACHE_NAME = 'budgetbuddy-v4';

// СТАЛО:
const CACHE_NAME = 'budgetbuddy-v5'; // ⚠️ ОБЯЗАТЕЛЬНО обновить при изменении кода!
```

### Шаг 2: Обновить версию в регистрации Service Worker

**Файл:** `client/index.html`

```javascript
// БЫЛО:
const SW_VERSION = 'v4'; // ⚠️ ОБЯЗАТЕЛЬНО обновить при изменении кода!

// СТАЛО:
const SW_VERSION = 'v5'; // ⚠️ ОБЯЗАТЕЛЬНО обновить при изменении кода!
```

### Шаг 3: Закоммитить и задеплоить

```bash
git add client/public/sw.js client/index.html
git commit -m "chore: update PWA cache version to v5"
git push origin main
```

---

## ❓ Почему это важно?

Без обновления версии кэша:
- ❌ Пользователи НЕ получат новые изменения автоматически
- ❌ Service Worker будет использовать старый кэш
- ❌ Изменения в коде не отобразятся у пользователей

С обновлением версии:
- ✅ Service Worker обнаружит новую версию
- ✅ Старый кэш будет очищен автоматически
- ✅ Пользователи получат обновление при следующем визите (в течение 60 секунд)

---

## 📋 Чеклист перед каждым деплоем

Перед каждым деплоем изменений в код проверь:

- [ ] Обновлена ли версия `CACHE_NAME` в `client/public/sw.js`?
- [ ] Обновлена ли версия `SW_VERSION` в `client/index.html`?
- [ ] Версии совпадают в обоих файлах?
- [ ] Изменения закоммичены?

---

## 🔍 Как проверить, что обновление работает

1. Открой DevTools → Application → Service Workers
2. Проверь, что новая версия Service Worker установлена
3. Проверь, что старый кэш удален (Application → Cache Storage)
4. Перезагрузи страницу - изменения должны появиться

---

## 📝 Примеры версий

- `v1` - Первая версия PWA
- `v2` - Обновление UI
- `v3` - Исправление багов
- `v4` - Добавление InstallPrompt
- `v5` - Следующее обновление...

**Правило:** Увеличивай версию на 1 при каждом деплое изменений в код.

---

## 🚨 Что делать, если забыл обновить версию?

Если уже задеплоил без обновления версии:

1. **Срочно обнови версию** в обоих файлах
2. **Сделай новый коммит** с обновлением версии
3. **Задеплой снова** - пользователи получат обновление при следующем визите

---

## 💡 Автоматизация (будущее)

В будущем можно автоматизировать это через:
- Pre-commit hook, который проверяет изменение версии
- CI/CD скрипт, который автоматически увеличивает версию
- GitHub Action, который проверяет совпадение версий

Но пока это делается вручную - **НЕ ЗАБЫВАЙ!**

---

**Последнее обновление:** 2026-01-19  
**Текущая версия:** v4
