✅ Ответы на вопросы
1. Voice Recognition
Короткий ответ: Нужен отдельный OPENAI_API_KEY ❗

Почему:

Anthropic API НЕ поддерживает audio transcription
Whisper (OpenAI) - лучшее решение для voice→text
У TogetherBudget это работает отлично
Рекомендация:

MVP (первый этап): БЕЗ voice ✅
→ Verification + текст + OCR
v2 (после MVP): Добавить voice
→ Попросить OPENAI_API_KEY у пользователя

Код для voice (когда понадобится):

import OpenAI from 'openai';
const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY });
const transcription = await openai.audio.transcriptions.create({
  file: audioFile,
  model: "whisper-1",
  language: "ru", // или auto-detect
});

2. Приоритеты фич
Фаза 1: MVP (2-3 часа работы) 🔥

✅ Database schema (verification codes)
✅ API endpoints (/generate-code, /verify)
✅ Telegram bot init + /start, /verify
✅ Текстовые команды ("потратил 500 на еду")
✅ Receipt OCR (Anthropic Vision)
✅ Frontend UI (Settings → получить код)

Фаза 2: Improvements (1-2 часа)

🎯 Inline keyboard (подтверждение транзакций)
🎯 Команды /balance, /stats
🎯 Multi-language (RU/EN)
🎯 Category auto-suggestions (ML)

Фаза 3: Advanced (опционально)

💡 Voice recognition (нужен OpenAI key)
💡 Уведомления (budget alerts)
💡 Recurring reminders
💡 Gamification

Начни с Фазы 1, остальное добавишь потом! 🚀

3. Код из старого приложения
ДА, вот что ОЧЕНЬ полезно:

📄 1. Parser (текстовые команды)
// telegram-parser.ts - ГОТОВАЯ логика:
// "потратил 500 на еду" → { amount: 500, category: "еда" }
// "spent 20 on coffee" → { amount: 20, category: "coffee" }
// "заработал 50000 зарплата" → { amount: 50000, type: "income" }

Где взять:

server/telegram-parser.ts (392 строки)
Функция: parseExpenseMessage(text, userId)
Возвращает: { amount, category, description, type }
📄 2. OCR Implementation (Receipt scanning)
// ocr-service.ts - Anthropic Vision API
// Фото чека → распознаёт сумму, магазин, дату

Где взять:

server/ocr-service.ts
Использует Anthropic Claude vision
Парсит чеки, возвращает структурированные данные
📄 3. Category Detection (ML)
// Автоматически предлагает категорию:
// "Starbucks" → "Еда и напитки"
// "Uber" → "Транспорт"

Где взять:

server/storage.ts → suggestCategory() (строка ~1800)
Использует merchantCategories для обучения
📄 4. Notifications (когда отправлять)
// alert-service.ts - логика уведомлений:
// - Превышен бюджет на категорию
// - Приближается planned purchase
// - Низкий баланс кошелька

Где взять:

server/alert-service.ts
server/notification-service.ts
📦 Что отправить разработчику
Минимальный набор для старта:

✅ server/telegram-parser.ts - парсинг команд
✅ server/ocr-service.ts - OCR чеков (Anthropic)
✅ server/storage.ts (только suggestCategory()) - ML категории
✅ Структура команд из telegram-bot.ts (как reference)
Опционально (для Фазы 2-3):

server/telegram-config.ts - конфигурация категорий
server/alert-service.ts - уведомления
server/notification-service.ts - отправка в Telegram
🎯 Итоговая рекомендация
ДЛЯ БЫСТРОГО СТАРТА:

Начни с Фазы 1 (без voice, без notifications)
Используй готовый parser из TogetherBudget
OCR через Anthropic (уже есть API key)
Voice оставь на потом (попросишь OPENAI_API_KEY у пользователя)