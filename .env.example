# ===== BudgetBot Environment Variables =====
# Copy this file to .env and fill in your values
# See ENV_VARIABLES_GUIDE.md for detailed documentation

# ===== Node Environment =====
NODE_ENV=development
# Options: development, production, test

# ===== Server Configuration =====
PORT=5000
# Server port (default: 5000)

# ===== Database (REQUIRED) =====
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/budget_bot
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database

# ===== Security (REQUIRED) =====
# IMPORTANT: Generate strong secrets before deployment!
# Generate SESSION_SECRET: openssl rand -base64 32
# Generate ENCRYPTION_KEY: openssl rand -base64 32

SESSION_SECRET=your_super_secret_key_here_change_in_production_min_32_chars
# Session secret for cookie signing (minimum 32 characters)
# Example: kX8hF3mN9pQ2rT5wY7zA1bC4dE6fG8hJ0kL2mN4pQ6r=

ENCRYPTION_KEY=your_32_byte_encryption_key_in_base64_format_44chars
# Encryption key for API keys (exactly 44 characters, 32 bytes in base64)
# Example: YWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFh

# ===== Telegram Bot (Optional) =====
# TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
# Get from @BotFather on Telegram

# Webhook mode (production, faster than polling)
# TELEGRAM_USE_WEBHOOK=true
# TELEGRAM_WEBHOOK_URL=https://your-app.com
# Leave commented for development (uses polling)

# ===== Frontend URL (Optional) =====
# FRONTEND_URL=https://your-app.com
# Required for webhooks (production only)

# ===== Redis Cache (Optional) =====
# Redis is used for caching categories, wallets, and exchange rates
# Improves performance by reducing database queries
# Leave commented to run without cache (app will work fine)

# REDIS_HOST=localhost
# Redis server host (default: localhost)

# REDIS_PORT=6379
# Redis server port (default: 6379)

# REDIS_PASSWORD=
# Redis password (if authentication is enabled)

# REDIS_DB=0
# Redis database number (default: 0)

# ===== Monitoring (Optional) =====
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# Sentry DSN for server-side error tracking
# Get from: https://sentry.io/ > Settings > Projects > [Your Project] > Client Keys (DSN)

LOG_LEVEL=info
# Logging level: error, warn, info, debug, trace

# ===== Client-side Variables (Optional) =====
# Only variables prefixed with VITE_ are exposed to the client

# VITE_API_URL=https://api.your-app.com
# API base URL (optional, defaults to same origin)

# VITE_SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx
# Sentry DSN for client-side error tracking

# VITE_ENABLE_ANALYTICS=false
# Enable client-side analytics
